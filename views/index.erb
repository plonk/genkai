<div class="container">

<h1><%= @title %></h1>

<div class="list-group">
<% @boards.each do |board| %>
  <a class="list-group-item"href="/<%= board.id %>/"><%= board.title %></a>
<% end %>
</div>

</div>
